
---

## 1️⃣ **DTO Layer (Data Transfer Objects) - طبقة نقل البيانات**

### 📁 **DTO/Department/**

#### **📄 DepartmentsDTO.cs**
كائن بسيط لعرض قائمة الأقسام (Get All).

**الخصائص:**
- `DeptId` → رقم القسم
- `Name` → اسم القسم
- `Code` → كود القسم
- `description` → وصف القسم
- `DateOfCreation` → تاريخ الإنشاء

💡 **ببساطة:** يُستخدم لعرض القائمة الكاملة للأقسام بدون تفاصيل زيادة.

---

#### **📄 DepartmentDetailsDTO.cs**
كائن مفصّل لعرض قسم واحد بكل التفاصيل (Get By Id).

**الخصائص الإضافية:**
- `CreatedBy` → مين اللي أنشأ القسم
- `LastModificationBy` → مين اللي عدّل آخر مرة
- `LastModeficationOn` → تاريخ آخر تعديل
- `IsDeleted` → هل القسم محذوف (Soft Delete)

💡 **ببساطة:** يُستخدم لعرض تفاصيل قسم واحد بالكامل (للصفحة التفصيلية).

---

#### **📄 CreatedDepartmentDTO.cs**
كائن لإنشاء قسم جديد (Add).

**الخصائص:**
- `Name` → **إلزامي** (Required)
- `Code` → **إلزامي** (Required)
- `description` → اختياري
- `DateOfCreation` → اختياري

**التحققات:**
- فيه `[Required]` على Name و Code عشان يتأكد المستخدم أدخلهم.

💡 **ببساطة:** البيانات اللي المستخدم يدخلها في الـ Form عشان يضيف قسم جديد.

---

#### **📄 UpdatedDepartmentDTO.cs**
كائن لتعديل قسم موجود (Update).

**الخصائص:**
- `DeptId` → رقم القسم المراد تعديله
- `Name`, `Code`, `description`, `DateOfCreation` → البيانات الجديدة

💡 **ببساطة:** البيانات اللي المستخدم يدخلها عشان يعدّل قسم موجود.

---

## 2️⃣ **Factories Layer - طبقة المصانع (Mapping)**

### **📄 DepartmentFactory.cs**
كلاس Static فيه دوال Extension لتحويل البيانات بين الـ Entity والـ DTO.

**الدوال:**

1. **`ToDepartmentDto()`** → تحوّل `Department` (Entity) إلى `DepartmentsDTO`
   - تُستخدم في GetAll عشان ترجع قائمة مبسطة.

2. **`ToDepartmentDetailsDto()`** → تحوّل `Department` إلى `DepartmentDetailsDTO`
   - تُستخدم في GetById عشان ترجع التفاصيل الكاملة.

3. **`ToEntity(CreatedDepartmentDTO)`** → تحوّل DTO لـ Entity عند الإضافة
   - تاخد البيانات من الـ Form وتحوّلها لكيان جاهز للحفظ في الداتا بيز.

4. **`ToEntity(UpdatedDepartmentDTO)`** → تحوّل DTO لـ Entity عند التعديل
   - تاخد البيانات الجديدة وتحوّلها لكيان جاهز للتحديث.

💡 **ببساطة:** المصنع ده بيحوّل البيانات من شكل لشكل (Mapping) بدل ما تكتب كود التحويل كل مرة.

---

## 3️⃣ **Services Layer - طبقة الخدمات**

### 📁 **Services/Interfaces/**

#### **📄 IDepartmentService.cs**
الـ Interface اللي بيحدد العمليات المتاحة على الأقسام.

**الدوال:**
- `GetAllDepartments()` → جلب كل الأقسام
- `GetDepartmentById(int id)` → جلب قسم معين
- `AddDepartment(CreatedDepartmentDTO)` → إضافة قسم جديد
- `UpdeteDepartment(UpdatedDepartmentDTO)` → تعديل قسم
- `DeleteDepartment(int id)` → حذف قسم

💡 **ببساطة:** بيحدد "إيه العمليات اللي أقدر أعملها على الأقسام" بدون تطبيق فعلي.

---

### 📁 **Services/Classes/**

#### **📄 DepartmentService.cs**
التطبيق الفعلي لـ `IDepartmentService`.

**الوظيفة:**
- بياخد `IDepartmentRepository` من الـ DAL عبر Dependency Injection.
- بينفّذ العمليات (CRUD) ويستخدم الـ Factory للتحويل بين Entity و DTO.

**أمثلة:**
- `GetAllDepartments()` → بيجيب كل الأقسام من الـ Repository ويحوّلها لـ DTOs باستخدام `ToDepartmentDto()`.
- `AddDepartment()` → بيحوّل الـ DTO لـ Entity باستخدام `ToEntity()` ويحفظه في الداتا بيز.
- `DeleteDepartment()` → بيدوّر على القسم، لو موجود بيحذفه.

💡 **ببساطة:** الكلاس ده بيطبّق منطق الأعمال (Business Logic) الخاص بالأقسام.

---

## ✅ **الترتيب المنطقي لفهم BLL**

| الترتيب | الملف/المجلد | الوصف |
|---------|-------------|-------|
| **1** | **DTO/Department/** | كائنات نقل البيانات (للعرض/الإدخال/التعديل) |
| **2** | **Factories/DepartmentFactory.cs** | تحويل البيانات بين Entity و DTO (Mapping) |
| **3** | **Services/Interfaces/** | تحديد العمليات المتاحة (Contracts) |
| **4** | **Services/Classes/** | التطبيق الفعلي لمنطق الأعمال |

---

## 🔄 **مثال على سير العمل الكامل (Get Department By Id)**
